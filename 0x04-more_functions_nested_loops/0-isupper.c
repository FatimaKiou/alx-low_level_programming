#include "main.h"
/**
 * _isupper - the function
 * @c: the value of the function
 * Return: return the result
 */
int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
